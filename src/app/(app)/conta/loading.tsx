/**
 * O esqueleto do v2 é o do grupo `(app)` — a tabela genérica. Esta fronteira
 * existe para `carregamento.test.ts` (toda tela com banco tem a sua) e aponta
 * para o mesmo desenho: o `Esqueleto` antigo repetia a navegação dentro da
 * casca nova.
 */
export { default } from '../loading'
