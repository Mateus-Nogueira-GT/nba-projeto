/**
 * `demo:seed` é um APELIDO de `demo:temporada`.
 *
 * O nome sobrevive porque o runbook e a memória do parceiro o conhecem — quem
 * digitar `npm run demo:seed` antes de uma apresentação recebe a temporada
 * simulada, não um erro de script inexistente. O seed roteirizado de um dia
 * (`semearDemo`) continua no repositório, mas só como fixture dos exemplos
 * numéricos do documento do CJ nas suítes de teste; nada em produção o chama.
 *
 * Importar executa: `demo-temporada.ts` roda no carregamento do módulo.
 */
import './demo-temporada'
